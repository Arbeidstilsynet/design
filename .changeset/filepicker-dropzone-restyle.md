---
'@arbeidstilsynet/design-css': patch
---

**FilePicker**: Restyle the dropzone to match the updated Gnist design. The outline and hover/drag background now use the main (accent) palette instead of blue, the default background uses `neutral/surface-default`, the hover state keeps the dashed border, and the drag state drops the elevation shadow.
