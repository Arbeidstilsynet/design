---
"@arbeidstilsynet/design-react": minor
"@arbeidstilsynet/design-css": minor
---

**LightDarkImage**: new component that replaces `LightDarkLogo`. `LightDarkImage` can be used with any pair of images to alternate based on current color scheme. `LogoBlack`, `LogoWhite`, `LogoGreen` are still exported.
