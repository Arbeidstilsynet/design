name: Deploy Storybook preview

on:
  push:
    branches:
      - main
      - feat/deploy-nais-preview

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  deploy_dev:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/deploy-storybook-nais.yml
    with:
      cluster: dev
      vars: .nais/vars-dev.yml
